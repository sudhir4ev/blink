# Storage (Postgres + Redis) — see libs/storage/src/config/storage-env.schema.ts

# Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DATABASE=blink

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
# Optional; omit if Redis has no password
# REDIS_PASSWORD=
REDIS_DB=0
